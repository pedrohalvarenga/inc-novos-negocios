import PropostasContent from "@/components/propostas/PropostasContent";

export const metadata = { title: "Propostas — INC Novos Negócios" };

export default function PropostasPage() {
  return <PropostasContent />;
}

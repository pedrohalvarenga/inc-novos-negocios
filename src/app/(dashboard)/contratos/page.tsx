import ContratosContent from "@/components/contratos/ContratosContent";

export const metadata = { title: "Contratos — INC Novos Negócios" };

export default function ContratosPage() {
  return <ContratosContent />;
}
